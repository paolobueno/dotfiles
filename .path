#!/usr/bin/env bash
export PATH="${PATH}:${HOME}/bin"

export GOPATH="${HOME}/go"
export PATH="${PATH}:${GOPATH}/bin"

# mac mysql client from
# brew install mysql-client
export PATH="${PATH}:/usr/local/opt/mysql-client/bin"
